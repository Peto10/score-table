services:
  score-table:
    image: score-table:0.1.0
    build: .
    ports:
      - "${HOST_PORT:-8080}:8080"
    environment:
      ADDR: ":8080"
      DB_PATH: "/data/app.db"
    volumes:
      - ./data:/data
    restart: unless-stopped

